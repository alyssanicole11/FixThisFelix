
  # Fix This Felix

  This is a code bundle for Fix This Felix. The original project is available at https://www.figma.com/design/EoYoHfWSr0A41garBsOsJk/Fix-This-Felix.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  